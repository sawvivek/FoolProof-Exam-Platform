# Secure Exam Client

This desktop shell opens the FoolProof candidate portal in a hardened demo window. It disables DevTools, popup windows and common browser navigation shortcuts. Production deployment should use an independently reviewed hardened client and OS-level device controls; this demo shell is not a security certification.
